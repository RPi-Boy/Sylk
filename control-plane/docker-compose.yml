version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    volumes:
      - ./tasks.db:/app/sylk_analytics.db
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
