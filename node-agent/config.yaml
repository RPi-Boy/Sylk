node_id: "local-node-01"
control_plane_url: "http://localhost:8000" # Replace with your friend's IP
redis_url: "redis://localhost:6379"     # Replace with your friend's IP
max_cpu_threshold: 85.0
polling_interval: 2
arch: "default"
